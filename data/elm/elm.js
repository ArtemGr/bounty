//@ts-check

// https://www.kaggle.com/robertbm/extreme-learning-machine-example

// ⌥ Experiment with importing CommonJS code from ES module,
//    cf. https://nodejs.org/api/packages.html
// ⌥ See if we can import https://github.com/tensorflow/tfjs-examples/blob/master/mnist-node/data.js

// https://youtu.be/hqvAfLlmWw4 ES modules

import * as tf from '@tensorflow/tfjs';

/*

const {assert} = require ('console');
const date = require ('date-and-time');
const fs = require ('fs');
const fsp = fs.promises;
const {knuthShuffle} = require ('knuth-shuffle');  // https://stackoverflow.com/a/2450976/257568
const {log, snooze} = require ('log');
const os = require ('os');



exports.test = async function() {
  log (`TBD`)}

function help() {
  console.log ('npm i && node elm.js --TBD')}

// When invoked from console, “npm i && node elm.js --tracks”
// cf. https://nodejs.org/dist/latest-v15.x/docs/api/modules.html#modules_accessing_the_main_module
if (require.main === module) (async () => {
  if (process.argv.includes ('--help')) {help(); return}

  if (process.argv.includes ('--TBD')) {
    log (`TBD`)
    return}
})()

*/